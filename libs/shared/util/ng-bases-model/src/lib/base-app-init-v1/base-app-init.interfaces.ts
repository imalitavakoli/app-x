/**
 * Halted step that prevented the app from being initialized.
 *
 * @export
 * @interface V1BaseAppInit_HaltedState
 * @typedef {V1BaseAppInit_HaltedState}
 */
export interface V1BaseAppInit_HaltedState {
  haltedStep: string;
}
