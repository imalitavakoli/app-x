# shared-util-ng-bases-model

This lib is meant to ONLY hold Base Interfaces/Types/Mocks.  
It's a PURELY independent lib.

## Running unit tests

Run `nx test shared-util-ng-bases-model` to execute the unit tests.
