# shared-util-ng-bases-consumer

This lib is meant to ONLY hold Base Services/Components that consume Base-TS-classes ("Bases" lib).  
It's a dependent lib to "Bases" lib.

## Running unit tests

Run `nx test shared-util-ng-bases-consumer` to execute the unit tests.
