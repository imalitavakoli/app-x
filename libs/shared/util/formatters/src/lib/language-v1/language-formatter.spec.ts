import { sharedUtilFormatters } from './formatters';

describe('sharedUtilFormatters', () => {
  it('should work', () => {
    expect(sharedUtilFormatters()).toEqual('shared-util-formatters');
  });
});
