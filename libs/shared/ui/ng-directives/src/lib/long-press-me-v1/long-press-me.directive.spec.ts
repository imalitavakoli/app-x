import { V1LongPressMeDirective } from './long-press-me.directive';

describe('LongPressMeDirective', () => {
  it('should create an instance', () => {
    const directive = new V1LongPressMeDirective();
    expect(directive).toBeTruthy();
  });
});
