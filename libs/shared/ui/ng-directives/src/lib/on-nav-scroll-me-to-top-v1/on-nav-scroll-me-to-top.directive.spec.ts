import { V1OnNavScrollMeToTopDirective } from './on-nav-scroll-me-to-top.directive';

describe('OnNavScrollMeToTopDirective', () => {
  it('should create an instance', () => {
    const directive = new V1OnNavScrollMeToTopDirective();
    expect(directive).toBeTruthy();
  });
});
