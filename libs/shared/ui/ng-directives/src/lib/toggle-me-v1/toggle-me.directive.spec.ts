import { V1ToggleMeDirective } from './toggle-me.directive';

describe('ToggleMeDirective', () => {
  it('should create an instance', () => {
    const directive = new V1ToggleMeDirective();
    expect(directive).toBeTruthy();
  });
});
