import { V2Config_MapUiNav } from '@x/shared-map-ng-config';

/**
 * Interface of the app's navigation item.
 *
 * @export
 * @typedef {V1AppHeaderDesktop_Nav}
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface V1AppHeaderDesktop_Nav extends V2Config_MapUiNav {}
