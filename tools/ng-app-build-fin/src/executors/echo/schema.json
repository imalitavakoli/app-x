{
  "$schema": "https://json-schema.org/schema",
  "version": 2,
  "title": "Echo executor",
  "description": "",
  "type": "object",
  "properties": {
    "changelog": {
      "type": "string",
      "description": "Path to the CHANGELOG file of the project."
    },
    "environmentsPath": {
      "type": "string",
      "description": "Path to the environments folder of the project."
    },
    "projectName": {
      "type": "string",
      "description": "Project name."
    },
    "projectBuildTarget": {
      "type": "string",
      "description": "Project build command (target) to run and compile the project."
    },
    "handleGit": {
      "type": "boolean",
      "description": "Define whether the Git related steps should be completed or skipped.",
      "default": false
    }
  },
  "required": [
    "changelog",
    "environmentsPath",
    "projectName",
    "projectBuildTarget"
  ]
}
