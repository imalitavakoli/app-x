# ng-app-build-fin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ng-app-build-fin` to build the library.

## Running unit tests

Run `nx test ng-app-build-fin` to execute the unit tests via [Jest](https://jestjs.io).
