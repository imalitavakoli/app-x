# Functionality docs (`docs/x/`)

Per-functionality PRD/TSD trees live here as `docs/x/{domain}/{name}/` when a functionality is documented. This directory is the registry root; it may be empty until the first functionality is added.

