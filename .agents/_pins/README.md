# Pins

Version-locked third-party artifacts this workspace intentionally freezes.

| Subfolder | Holds |
| --- | --- |
| `plugins/` | Agent plugins (Superpowers, …) — see `plugins/README.md` |

Add a sibling folder (for example `mcp/`) only when something that is not a plugin must be SHA-pinned the same way.
